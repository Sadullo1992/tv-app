import { FindCountryByCodePipe } from './find-country-by-code.pipe';

describe('FindCountryByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FindCountryByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
