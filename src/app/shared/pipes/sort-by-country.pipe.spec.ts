import { SortByCountryPipe } from './sort-by-country.pipe';

describe('SortByCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
