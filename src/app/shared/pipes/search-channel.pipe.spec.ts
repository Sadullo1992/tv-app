import { SearchChannelPipe } from './search-channel.pipe';

describe('SearchChannelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchChannelPipe();
    expect(pipe).toBeTruthy();
  });
});
